# Backend URL Configuration
# This URL is used by the frontend JavaScript to connect to the backend API
# Default: http://localhost:8080 (works when running locally with docker-compose)
# For production, set this to your actual backend URL (e.g., https://api.example.com)
BACKEND_URL=http://localhost:8080
